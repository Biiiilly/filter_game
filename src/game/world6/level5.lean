import game.world6.level4 -- hide

open set -- hide

variables {α : Type*} [topological_space α] -- hide

/-

# Topology World

## Level 5 : `is_open.mem_nhds_iff`

-/

/- Lemma
This lemma is basically from previous levels.
-/
lemma is_open.mem_nhds_iff {a : α} {s : set α} (hs : is_open s) : 
  s ∈ (𝓝 a) ↔ a ∈ s :=
begin
  sorry












end