import game.world6.level2 -- hide

open set -- hide

variables {α : Type*} [topological_space α] -- hide

/-

# Topology World

## Level 3 : `mem_of_mem_nhds`

-/

/- Lemma
Try these exercises:
-/
lemma mem_of_mem_nhds {a : α} {s : set α} : s ∈ (𝓝 a) → a ∈ s :=
begin
  sorry












end