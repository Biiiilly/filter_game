import game.world4.level6 -- hide

open set -- hide

variable {α : Type*} -- hide

/-

# Principal Filter World

## Level 7 : `filter.principal_univ`

Now, go back to our first goal: 𝓟 (univ : set α) = ⊤.
Hint: 'lattice.top_unique' is a good start, and you can find it on the left.
-/

/- Lemma
The principal filter of the whole set is the top.
-/
lemma filter.principal_univ : 𝓟 (univ : set α) = ⊤ :=
begin
  sorry








end

/- Axiom : top_unique (h : ⊤ ≤ a)
a = ⊤
-/