import game.world4.level1 -- hide

open set -- hide

variable {α : Type*} -- hide

/-

# Principal Filter World

## Level 2 : `filter.le_principal_iff`

-/

/- Lemma
A filter f is finer than the principal filter of s if and only if s ∈ f.
-/
lemma filter.le_principal_iff {s : set α} {f : filter α} : f ≤ 𝓟 s ↔ s ∈ f :=
begin
  sorry












end